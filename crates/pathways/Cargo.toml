[package]
name = "truent-pathways"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true

description = "Truent: the security-pathway map — routes every class of software, web, system and cloud security to native detectors, hosted skills, or an explicit assessment checklist; generates STRIDE threat models from discovered structure."

keywords = ["truent", "security", "threat-model", "assessment", "coverage"]
categories = ["development-tools"]
readme = "README.md"

[dependencies]
truent-core.workspace = true
regex.workspace = true
lazy_static.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
